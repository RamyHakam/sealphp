sudo: required

language: php

dist: trusty

php:
   - 7.2
   - 7.3
   
before_script:
  composer install
script: composer test

after_script:
  echo "no tests erros"

after_failure: echo "errors in tests"
